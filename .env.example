# Required for deployed/VPS: shared secret for app login (users enter this to access the app)
# SHARED_SECRET=your-secret-here

# Optional: Google Geocoding API key. When set, geocoding uses Google instead of Nominatim
# (faster, no 1 req/sec limit; first 10k requests/month free, then ~$0.005/request).
# Get a key: https://console.cloud.google.com/google/maps-apis/
# GOOGLE_GEOCODING_API_KEY=your_api_key_here

# On VPS: set by deploy/setup-vps.sh (systemd) to browser-profile/ for shared Google My Maps account
# BROWSER_USER_DATA_DIR=/opt/address-plotter/browser-profile
