# Multi-user configuration (required for deployed/VPS)
# Define users as JSON array. Each user gets their own isolated Google profile.
# Format: [{"id":"username","name":"Display Name","pin":"1234"}, ...]
# APP_USERS=[{"id":"alice","name":"Alice","pin":"1234"},{"id":"bob","name":"Bob","pin":"5678"}]

# Optional: Legacy shared secret (if APP_USERS is not set, falls back to single-user mode)
# SHARED_SECRET=your-secret-here

# Optional: Google Geocoding API key. When set, geocoding uses Google instead of Nominatim
# (faster, no 1 req/sec limit; first 10k requests/month free, then ~$0.005/request).
# Get a key: https://console.cloud.google.com/google/maps-apis/
# GOOGLE_GEOCODING_API_KEY=your_api_key_here

# Profile directory base (default: ./profiles on VPS, each user gets ./profiles/{userId}/)
# PROFILES_BASE_DIR=/opt/address-plotter/profiles
